In the backends scenario we use flask for communication between the frontend and database

Advisely to create an virtual environment first. This READme will update later, for the use of flask. Right now haven't updated much.

`cd Backend` change dir to Backend
`python -m venv .venv` To create virutal environemnt
`pip install requirements.txt` *Not updated requirements.txt yet.